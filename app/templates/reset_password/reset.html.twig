{% extends 'base.html.twig' %}

{% block title %}{{ 'title.reset_password'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'title.reset_password'|trans }}</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.password) }}
        <button class="btn btn-primary">{{ 'action.reset_password'|trans }}</button>
    {{ form_end(resetForm) }}
{% endblock %}
