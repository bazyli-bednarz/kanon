{% include 'default/_form.html.twig' with {
    page_title: 'title.piece_edit'|trans({'%name%': piece.name|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'piece_index',
} %}