{% include 'default/_form.html.twig' with {
    page_title: 'title.composer_edit'|trans({'%name%': composer.name|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'composer_index',
} %}