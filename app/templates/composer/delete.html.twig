{% include 'default/_form.html.twig' with {
    page_title: 'title.composer_delete'|trans({'%name%': composer.name|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'composer_index',
} %}
