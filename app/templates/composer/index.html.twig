{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.composer_list'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.composer_list'|trans }}
        <button class="btn-learn-more" id="info-show-hide" title="{{ 'action.learn_more'|trans }}" aria-label="{{ 'action.learn_more'|trans }}"><i class="fa-solid fa-circle-question"></i></button></h1>
    <div class="image-wrapper hidden" id="info-box-hideable">
        <div class="image-wrapper__content">
            <img src="{{ asset('images/undraw_quick_chat.svg') }}" alt="{{ 'image.quick_chat'|trans }}"
                 class="image--medium">
            <p>{{ 'info.composers'|trans }}</p>
        </div>
    </div>
    <a class="btn btn-primary btn-lg" href="{{ url('composer_create') }}">{{ 'action.add_composer'|trans }} <i class="fa-solid fa-plus"></i></a>
    {% include 'default/_composers_list.html.twig' with {
        pagination: pagination
    } %}
{% endblock %}