{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.scale_details'|trans({'%name%': scale.name|trans|default('')}) }}
{% endblock %}

{% block body %}
    <h1 class="underline-header">{{ 'title.scale_details'|trans({'%name%': scale.name|trans|default('')}) }}</h1>
    {% if scale is defined and scale|length %}
        <dl class="dl-horizontal">
            <dt>{{ 'label.scale_name'|trans }}</dt>
            <dd>{{ scale.name|trans }}</dd>
        </dl>

        {% include 'default/_pieces_list.html.twig' with {
            pagination: pagination,
        } %}

        <p>
            <a href="{{ url('scale_index') }}" title="{{ 'action.back_to_list'|trans }}">
                {{ 'action.back_to_list'|trans }}
            </a>
        </p>
    {% else %}
        <p>
            {{ 'message.item_not_found'|trans }}
        </p>
    {% endif %}
{% endblock %}